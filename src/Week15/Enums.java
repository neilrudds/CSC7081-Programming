package Week15;

/**
 * Enums
 * @author neilr
 */

public class Enums {

    // Defining an Enum
    // With Java, you define an enum type by using the enum keyword.
    // For example, you would specify days-of-the-week enum type as:

    /**
     * Enum list for days of the week
     */
    public enum Day {
        SUNDAY, MONDAY, TUESDAY, WEDNESDAY,
        THURSDAY, FRIDAY, SATURDAY
    }
}
